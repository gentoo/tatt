# Code for stabeling @@EBUILD@@
cd @@CP@@

ekeyword @@ARCH@@ @@EBUILD@@
echangelog "@@ARCH@@ stable per bug @@BUG@@" || {
	echo " *** echangelog failed"
	exit 1
}
repoman manifest 
repoman -d full || exit 1
repoman -m "@@ARCH@@ stable per bug @@BUG@@" commit || exit 1

cd ../..
## @@CP@@ done